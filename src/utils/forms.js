const mlInput = {
  maxlength: "9",
  inputmode: "decimal",
  "unmasked-value": true,
  "reverse-fill-mask": true,
};

const abvInput = {
  maxlength: "5",
  inputmode: "decimal",
  "unmasked-value": true,
  "reverse-fill-mask": true,
};

const priceInput = {
  maxlength: "10",
  inputmode: "decimal",
  "unmasked-value": true,
  "reverse-fill-mask": true,
};

export { mlInput, abvInput, priceInput };
