const input = {
  "label-color": "dark",
  class: "inputs",
  borderless: true,
};

const roundButton = {
  class: "round-button full-width",
  "text-color": "dark",
  "no-caps": true,
  unelevated: true,
  padding: "sm",
};

const textButton = {
  color: "dark",
  flat: true,
  "no-caps": true,
  unelevated: true,
  rounded: true,
  dense: true,
  class: "full-width",
};

export { input, roundButton, textButton };
