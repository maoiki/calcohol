const input = {
  "label-color": "dark",
  class: "inputs",
  borderless: true,
};

const roundButton = {
  class: "round-button",
  "text-color": "dark",
  "no-caps": true,
  unelevated: true,
  padding: "sm",
};

const textButton = {
  color: "dark",
  flat:true,
  "no-caps": true,
  unelevated: true,
  rounded: true,
  dense: true,
};

export { input, roundButton, textButton };
