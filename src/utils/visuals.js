const input = {
  "label-color": "dark",
  "bg-color": "inputs",
  borderless: true,
};

const roundButton = {
  color: "round-button",
  "text-color": "dark",
  "no-caps": true,
  unelevated: true,
  padding: "sm",
};

export { input, roundButton };
